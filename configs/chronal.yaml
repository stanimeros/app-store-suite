app:
  name: Chronal
  flutter_dir: /Users/stanimeros/Developer/chronal
  bundle_id_ios: com.stanimeros.chronal
  package_id_android: com.stanimeros.chronal
  icon_source: assets/icon/icon.png # must be >=1024x1024

devices:
  ios_phone:
    simulator: "iPhone 17 Pro"
  ios_tablet:
    simulator: "iPad Pro 13-inch (M5)"
  android_phone:
    avd: "Medium_Phone"
  android_tablet:
    avd: "Pixel_Tablet"

# No fixed shot list: capture whatever screens you want, naming each one as you
# go (e.g. "home", "itinerary", "map"). Titles are suggested automatically by
# the `claude` CLI from the actual screenshot content and stored in
# output/titles.json, shared across all devices for the same shot id.

style:
  background_color: "#FAFAF8"
  title_color: "#1A1A1A"
  font_bold: "Poppins-Bold.ttf"
  font_regular: "Poppins-Regular.ttf"
